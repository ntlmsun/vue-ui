import MsButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
MsButtonGroup.install = function(Vue) {
  Vue.component(MsButtonGroup.name, MsButtonGroup);
};

export default MsButtonGroup;
