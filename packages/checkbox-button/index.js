import MsCheckboxButton from '../checkbox/src/checkbox-button';

/* istanbul ignore next */
MsCheckboxButton.install = function(Vue) {
  Vue.component(MsCheckboxButton.name, MsCheckboxButton);
};

export default MsCheckboxButton;
